import { useEffect } from "react";
const region = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
};

export const useChop = () => {
  const handleChop = () => {};
  useEffect(() => {}, []);
  return {
    handleChop,
  };
};
